#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int main()
{   int a[10],i,n;
scanf("%d",&n);
for(i=0;i<n;i++)
    {scanf("%d",&a[i]);
    a[i]=~--a[i];
    }
for(i=0;i<n;i++)   
    printf("%d\n",a[i]);
    
    return 0;
}
